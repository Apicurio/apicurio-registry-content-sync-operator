name: Build and Test Workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  build-test:
    name: Build and Test
    runs-on: ubuntu-18.04
    if: github.repository_owner == 'Apicurio'
    steps:

      - name: Install JDK 11
        uses: AdoptOpenJDK/install-jdk@v1
        with:
          version: 11
          architecture: x64

      - name: Checkout "${{ github.ref }}"
        uses: actions/checkout@v2

      - name: Build Apicurio Registry Deps
        run: |
          cd "${{ github.workspace }}/.."
          git clone https://github.com/Apicurio/apicurio-registry.git -o upstream apicurio-registry.git
          cd apicurio-registry.git
          git checkout master
          mvn install -am -pl client
          cd "${{ github.workspace }}"

      - name: Build
        run: |
          mvn package

      # - name: Build container
      #   run: |
      #     mvn clean package -pl sync -am --no-transfer-progress \
      #       -Dquarkus.container-image.build=true \
      #       -Dquarkus.container-image.push=true
